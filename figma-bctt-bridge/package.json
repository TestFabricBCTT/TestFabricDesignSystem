{
  "name": "figma-bctt-bridge",
  "version": "1.0.0",
  "description": "Figma plugin to receive commands from BCTT MCP Server and create pages/frames",
  "scripts": {
    "build": "npm run build:code && npm run build:ui",
    "build:code": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2015",
    "build:ui": "cp src/ui.html dist/ui.html",
    "watch": "npm run build && chokidar \"src/**/*\" -c \"npm run build\"",
    "dev": "npm run build"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.98.0",
    "esbuild": "^0.20.0",
    "chokidar-cli": "^3.0.0",
    "typescript": "^5.3.0"
  },
  "keywords": ["figma", "plugin", "bctt", "mcp", "design-system"],
  "author": "BCTT Agent Factory",
  "license": "MIT"
}
